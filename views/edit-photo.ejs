<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Photo - Berlin Fashion Blog</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1 class="centered-heading">Edit Photo</h1>
    </header>
    <section class="form-section">
        <form action="/edit-photo/<%= photo._id %>" method="POST">
            <input type="text" name="title" placeholder="Title" value="<%= photo.title %>" required>
            <select name="category" required>
                <option value="">Select Category</option>
                <option value="Spring/Summer" <%= photo.category === 'Spring/Summer' ? 'selected' : '' %>>Spring/Summer</option>
                <option value="Fall/Winter" <%= photo.category === 'Fall/Winter' ? 'selected' : '' %>>Fall/Winter</option>
            </select>
            <input type="url" name="url" placeholder="Image URL" value="<%= photo.url %>" required>
            <button type="submit">Update Photo</button>
        </form>
    </section>
</body>
</html>
